@model IEnumerable<Analyzer.Models.Device>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2>Tester Stage Device List</h2>
        </div>
    </div>

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Device Name</th>
                    <th>Serial Number</th>
                    <th>Model</th>
                    <th>Model Name</th>
                    <th>Comments</th>
                    <th>Other Comments</th>
                    <th>Total Count</th>
                    <th>Tester Time</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width=7%>@obj.Name</td>
                        <td width=7%>@obj.SN</td>
                        <td width=7%>@obj.Model</td>
                        <td width=7%>@obj.ModelName</td>
                        <td width=7%>@obj.Comments</td>
                        <td width=7%>@obj.OtherComments</td>
                        <td width=7%>@obj.TotalCount</td>
                        <td width=7%>@obj.TesterTime</td>
                        <th>Init Stage action</th>
                        <th>Next Stage action</th>
                    

                        <td class="text-center">
                            <div class="w075 btn-group" role="group">
                                <a asp-action="Edit" asp-route-Id="@obj.Id" class="btn btn-primary mx-2">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a asp-action="Delete" asp-route-Id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </div>
                        </td>
                        <td class="text-center">
                                <a asp-controller="SAnalyzer" asp-action="Create" asp-route-Id="@obj.Id" class="btn btn-success mx-2">
                                <i class="fa-solid fa-user-tie"></i> Create Analyzer Stage
                                </a>
                        </td>                        
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No devices</p>
    }
</div>